<%- include('./includes/head') %>
<%- include('./includes/nav') %>

    <div class="container">

        <div class="mt-3">
            <%- include('./includes/messages') %>
        </div>
        
        <div class="row">
            <div class="col-md-4 mx-auto">

                <% if(!user) { %>

                <div class="card my-4">
                    <div class="card-header card-login-header">
                        <div class="text-center my-3">
                            <h4 class="text-dark">Faça seu Login abaixo:</h4>
                        </div>
                    </div>
                    <div class="card-body card-login-body">
                        <form action="/login/login" method="post" class="form-group ">
                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                            <label>Email:</label>
                                <input class="form-control" type="text" name="email">
                            <label>Senha:</label>
                                <input class="form-control" type="password" name="senha">
                            <button class="form-control mt-3 btn btn-dark" type="submit">Submit</button>
                        </form>  
                    </div>
                </div>

                <% } %>

            </div>
        </div>
    </div>

<%- include('./includes/footer') %>